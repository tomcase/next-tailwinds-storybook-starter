import type { NextPage } from "next";
import { HomeTemplate } from "../components/Templates/HomeTemplate";

const Home: NextPage = () => {
	return (
		<main>
			<HomeTemplate />
		</main>
	);
};

export default Home;
